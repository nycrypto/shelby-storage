# Scripts

These scripts are wrappers around the Shelby CLI.

They do NOT store any secrets.
They expect your Shelby CLI to be configured locally (e.g. `~/.shelby/config.yaml`).

Default:
- context: shelbynet
- account: default_account
